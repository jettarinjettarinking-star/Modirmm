该文件夹下放的是语言文件。

添加语言文件的步骤:
1.仿照"Chinese.txt"写好一个语言文件。
2.在"LanguageID.txt"中声明语言文件的名字。

This folder contains language files.

Steps to add language files:
1. Write a language file based on "Chinese.txt".
2. Declare the name of the language file in "LanguageID.txt".